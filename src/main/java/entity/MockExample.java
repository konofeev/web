package entity;

import java.util.List;
import java.util.ArrayList;

public class MockExample
{
    private List<Example> examples;

    public MockExample()
    {
        examples = new ArrayList<Example>();
        examples.add
        (
            new Example
            (
                "Создание объекта коллекции HashMap",
                "Map<String, String> hashMap = new HashMap<String, String>();",
                "Создаём коллекцию с именем hashMap, основанную на хеш-таблицах. В качестве ключа и значения используются строковые значения."
            )
        );
        examples.add
        (
            new Example
            (
                "Создание объекта коллекции HashMap",
                "Map<String, String> hashMap = new HashMap<String, String>();",
                "Создаём коллекцию с именем hashMap, основанную на хеш-таблицах. В качестве ключа и значения используются строковые значения."
            )
        );
        examples.add
        (
            new Example
            (
                "Создание объекта коллекции HashMap",
                "Map<String, String> hashMap = new HashMap<String, String>();",
                "Создаём коллекцию с именем hashMap, основанную на хеш-таблицах. В качестве ключа и значения используются строковые значения."
            )
        );
        examples.add
        (
            new Example
            (
                "Создание объекта коллекции HashMap",
                "Map<String, String> hashMap = new HashMap<String, String>();",
                "Создаём коллекцию с именем hashMap, основанную на хеш-таблицах. В качестве ключа и значения используются строковые значения."
            )
        );
        examples.add
        (
            new Example
            (
                "Создание объекта коллекции HashMap",
                "Map<String, String> hashMap = new HashMap<String, String>();",
                "Создаём коллекцию с именем hashMap, основанную на хеш-таблицах. В качестве ключа и значения используются строковые значения."
            )
        );
        examples.add
        (
            new Example
            (
                "Создание объекта коллекции HashMap",
                "Map<String, String> hashMap = new HashMap<String, String>();",
                "Создаём коллекцию с именем hashMap, основанную на хеш-таблицах. В качестве ключа и значения используются строковые значения."
            )
        );
    }

    public List<Example> getExamples()
    {
        return examples;
    }
}
